package dev.liftorium.data

/**
 * Phase 1 placeholder so the `:data` module has a real Kotlin source file
 * for the Android Gradle Plugin to compile. Phase 4 (`android-program-runner`)
 * adds the first Room `@Database`, entities, DAOs, and exported schema JSON.
 */
internal object DataModuleMarker {
    const val PHASE: Int = 1
}
